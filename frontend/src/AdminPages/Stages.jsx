import React, { useEffect } from "react";
import { useAuth } from "../context/AuthProvider";

const Stages = () => {
  return (
    <div>
      <h1>Stages Page</h1>
    </div>
  );
};

export default Stages;