import React, { useEffect } from "react";
import { useAuth } from "../context/AuthProvider";

const Groups = () => {
  return (
    <div>
      <h1>Groups Page</h1>
    </div>
  );
};

export default Groups;